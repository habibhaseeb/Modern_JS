// This function take an array that contain 3 valuse first and second arr.
function valueChecker(a,b)  {

    let elements1 = ['A','H','K']; 
    let elements2 = ['B','I',"L"];

    let indexToShow = 0;

    if(elements1[0] > elements1[1]){   // Its show that which value is greater like 'A'

        console.log(indexToShow = 0);
    }
    else if (elements1[1] > elements1[0]) {
        
        return elements1[indexToShow]
    } 
  }
 let func = valueChecker(2+2)

    console.log(func); // print this function  in terminal window.
